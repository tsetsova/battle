<!DOCTYPE html>
<html>
<head>
  <title>
    Battle!
  </title>
  <link rel="stylesheet" type="text/css" href="bootstrap.css">
  <link rel="stylesheet" type="text/css" href="stylesheet.css">
</head>
<body>
  <div class="container-fluid">
    <div class="row text-center">
      <div class="col-md-10 col-xs-8">
				<h1>Hellooo <%=$game.player_1.name%> vs. <%=$game.player_2.name%></h1>

				<h2> <%=$game.opponent.name%>'s health: <%=$game.opponent.health%>hp </h2>

				<form action = "/attack" method = "post">
					<input type='submit' name = "attack" value="Attack <%=$game.opponent.name%>">
				</form>


				<%if @attack %>
					<h3>You attacked <%=$game.opponent.name%></h3>
					<h3><%=$game.opponent.name%>'s health is down 2 points</h3>
					<h3>Now it's <%=$game.turn.name%>'s turn to attack!</h3>
				<%end %>
			</div>
		</div>	
	</div>
</body>
</html>

